Fixture:

tests/fixtures/positive/edk_eval_pure_surface

Current blocker:

The fixture depends on `edk-eval` and `edk-workspace` as local path
dependencies. Package materialization now succeeds: `etas pkg update .`,
`etas pkg lock .`, and `etas check .` all pass.

Observed command:

- `etas run .` from this fixture fails with `analysis::InvalidArguments`
  because imported pure `tool` aliases such as `tool_compare_golden` do not
  name runtime values.

Expected source coverage:

- stable assertion and text diff diagnostics;
- trace expectation count and action/order matching;
- deterministic fixture/result/receipt mock constructors;
- pure result summaries without filesystem writes.

This fixture must not introduce a default eval-store handler, hidden file IO,
or a fake evaluation runner.
