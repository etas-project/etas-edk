[package]
name = "spec-impl-canonical-evidence-positive"
version = "0.1.0"
edition = "2026"
license = "MIT"
description = "Positive fixture proving canonical impl Type ~ Spec evidence satisfies a spec-bound flow"
repository = "https://example.invalid/etas-edk-fixtures/spec-impl-canonical-evidence-positive"

[source]
root = "src"

[dependencies]
std = { version = "0.1" }

[[bin]]
name = "spec-impl-canonical-evidence-positive"
module = "tests.edk.spec_impl_canonical_evidence.main"
flow = "main"
