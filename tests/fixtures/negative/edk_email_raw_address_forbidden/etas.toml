[package]
name = "edk-email-raw-address-forbidden"
version = "0.1.0"
edition = "2026"
license = "MIT"
description = "Negative fixture proving raw strings cannot stand in for EmailAddress"
repository = "https://example.invalid/etas-edk-fixtures/edk-email-raw-address-forbidden"

[source]
root = "src"

[dependencies]
std = { version = "0.1" }
edk_email = { path = "../../../../packages/edk-email", import = "edk.email" }

[[bin]]
name = "edk-email-raw-address-forbidden"
module = "tests.edk.negative.edk_email_raw_address_forbidden.main"
flow = "main"
