[package]
name = "edk-workspace-root-not-report-path"
version = "0.1.0"
edition = "2026"
license = "MIT"
description = "Negative fixture proving WorkspaceRootPath cannot satisfy WritablePath[ReportsRoot]"
repository = "https://example.invalid/etas-edk-fixtures/edk-workspace-root-not-report-path"

[source]
root = "src"

[dependencies]
std = { version = "0.1" }
edk_workspace = { path = "../../../../packages/edk-workspace", import = "edk.workspace" }

[[bin]]
name = "edk-workspace-root-not-report-path"
module = "tests.edk.negative.edk_workspace_root_not_report_path.main"
flow = "main"
