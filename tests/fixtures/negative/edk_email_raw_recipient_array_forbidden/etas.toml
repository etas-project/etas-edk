[package]
name = "edk-email-raw-recipient-array-forbidden"
version = "0.1.0"
edition = "2026"
license = "MIT"
description = "Negative fixture proving raw string arrays cannot stand in for deliverable email recipient evidence"
repository = "https://example.invalid/etas-edk-fixtures/edk-email-raw-recipient-array-forbidden"

[source]
root = "src"

[dependencies]
std = { version = "0.1" }
edk_email = { path = "../../../../packages/edk-email", import = "edk.email" }

[[bin]]
name = "edk-email-raw-recipient-array-forbidden"
module = "tests.edk.negative.edk_email_raw_recipient_array_forbidden.main"
flow = "main"
