[package]
name = "edk-email-raw-tls-smtp-endpoint-constructor-forbidden"
version = "0.1.0"
edition = "2026"
license = "MIT"
description = "Negative fixture proving TlsSmtpEndpoint evidence cannot be externally forged"
repository = "https://example.invalid/etas-edk-fixtures/edk-email-raw-tls-smtp-endpoint-constructor-forbidden"

[source]
root = "src"

[dependencies]
std = { version = "0.1" }
edk_email = { path = "../../../../packages/edk-email", import = "edk.email" }

[[bin]]
name = "edk-email-raw-tls-smtp-endpoint-constructor-forbidden"
module = "tests.edk.negative.edk_email_raw_tls_smtp_endpoint_constructor_forbidden.main"
flow = "main"
