[package]
name = "std-req-http-error-mapping-contract"
version = "0.1.0"
edition = "2026"
license = "MIT"
description = "Std requirements probe for EDK HTTP invalid request and transport error mapping contracts"
repository = "https://example.invalid/etas-edk-std-requirements/std-req-http-error-mapping-contract"

[source]
root = "src"

[dependencies]
std = { version = "0.1" }
edk_http = { path = "../../../../packages/edk-http", import = "edk.http" }

[[bin]]
name = "std-req-http-error-mapping-contract"
module = "std_requirements.http.positive.edk_http_error_mapping_contract.main"
flow = "main"
